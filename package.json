{
  "name": "sass",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build --host",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "sass": "^1.61.0",
    "vite": "^4.2.0"
  }
}
